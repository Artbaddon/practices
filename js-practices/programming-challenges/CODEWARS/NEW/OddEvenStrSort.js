function sortMyString(S) {
  const output = [];

  for (let i = 0; i < S.length; i++) {}
  return "";
}
function sortMyString(S) {
  const output = [];

  for (let i = 0; i < S.length; i++) {
    if (i % 2 !== 0) {
      console.log(S[i]);
    }
  }
  return "";
}
const S = "niaa";
let output = "";
for (let i = 0; i < S.length; i++) {
  if (i % 2 === 0) {
    output += S[i].toUpperCase();
  } else {
    output += S[i];
  }
}

const str = "capitan";

const obj = {
  a: "a",
  e: "e",
  i: "i",
  o: "o",
  u: "u",
};
