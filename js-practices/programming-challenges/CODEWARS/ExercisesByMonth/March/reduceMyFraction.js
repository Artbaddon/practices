function reduce(fraction) {
  //simplify me!
}

function findCommonDiv([numerator, denominator]) {
  console.log(numerator, denominator);
}

let fraction = [60, 120];
let num = fraction[0];
let divisors = [[], []];
let sqrtNum = Math.sqrt(num);
for (let i = 0; i <= sqrtNum; i++) {}
